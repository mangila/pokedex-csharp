{
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "test-app": "dotnet test pokedex.sln && npm run lint --prefix ./pokedex-ui",
    "build-app": "dotnet build pokedex.sln && npm run build --prefix ./pokedex-ui"
  }
}
